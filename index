<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Создай игру за 20 минут 🎮 | Уроки по Unity + Нейросетям</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800&display=swap');

    :root {
      --primary: #6e48ff;
      --secondary: #ff6b6b;
      --dark: #14141f;
      --light: #f9f9ff;
      --gradient: linear-gradient(135deg, #6e48ff, #ff6b6b);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      background: var(--dark);
      color: var(--light);
      overflow-x: hidden;
      position: relative;
    }

    /* Анимированный фон — звёздочки */
    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        radial-gradient(white, rgba(255,255,255,.2) 2px, transparent 10px);
      background-size: 100px 100px;
      background-position: 0 0, 50px 50px;
      animation: moveBg 20s linear infinite;
      z-index: -1;
      opacity: 0.3;
    }

    @keyframes moveBg {
      0% { background-position: 0 0, 50px 50px; }
      100% { background-position: 100px 100px, 150px 150px; }
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px;
    }

    header {
      text-align: center;
      padding: 80px 0 60px;
      position: relative;
    }

    h1 {
      font-size: 3.5rem;
      font-weight: 800;
      background: var(--gradient);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 20px;
      animation: fadeInUp 1s ease-out;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .subtitle {
      font-size: 1.3rem;
      font-weight: 400;
      max-width: 700px;
      margin: 0 auto 40px;
      line-height: 1.6;
      color: #ccc;
      animation: fadeInUp 1s ease-out 0.3s both;
    }

    .highlight {
      color: #fff;
      font-weight: 700;
    }

    /* Блок уроков */
    .lessons {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 30px;
      margin: 60px 0;
    }

    .lesson-card {
      background: rgba(30, 30, 50, 0.7);
      border-radius: 20px;
      padding: 30px;
      text-align: center;
      transition: all 0.4s ease;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(110, 72, 255, 0.3);
      position: relative;
      overflow: hidden;
    }

    .lesson-card::before {
      content: "";
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: var(--gradient);
      z-index: -1;
      animation: rotate 4s linear infinite;
      opacity: 0;
      transition: opacity 0.4s;
    }

    @keyframes rotate {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .lesson-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 30px rgba(110, 72, 255, 0.4);
    }

    .lesson-card:hover::before {
      opacity: 0.05;
    }

    .lesson-icon {
      font-size: 3rem;
      margin-bottom: 20px;
      background: var(--gradient);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .lesson-title {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 15px;
    }

    .lesson-desc {
      color: #bbb;
      margin-bottom: 25px;
      line-height: 1.6;
    }

    .price {
      font-size: 2rem;
      font-weight: 800;
      background: var(--gradient);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin: 20px 0;
    }

    .btn {
      display: inline-block;
      padding: 15px 30px;
      background: var(--gradient);
      color: white;
      font-weight: 700;
      border-radius: 50px;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(110, 72, 255, 0.4);
      border: none;
      cursor: pointer;
      font-size: 1rem;
    }

    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(110, 72, 255, 0.6);
    }

    .btn.pulse {
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0% { box-shadow: 0 5px 15px rgba(110, 72, 255, 0.4); }
      50% { box-shadow: 0 5px 15px rgba(255, 107, 107, 0.8); }
      100% { box-shadow: 0 5px 15px rgba(110, 72, 255, 0.4); }
    }

    /* Модальное окно */
    .modal {
      display: none;
      position: fixed;
      z-index: 100;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.8);
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background: #1e1e30;
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      max-width: 500px;
      animation: scaleIn 0.5s ease-out;
    }

    @keyframes scaleIn {
      from { transform: scale(0.8); opacity: 0; }
      to { transform: scale(1); opacity: 1; }
    }

    .modal h2 {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #6e48ff;
    }

    .close-btn {
      margin-top: 20px;
      background: #444;
    }

    footer {
      text-align: center;
      padding: 40px 0;
      color: #777;
      font-size: 0.9rem;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin-top: 60px;
    }

    @media (max-width: 768px) {
      h1 {
        font-size: 2.5rem;
      }
      .subtitle {
        font-size: 1.1rem;
      }
      .lessons {
        grid-template-columns: 1fr;
      }
    }

    /* Анимация появления блоков при скролле */
    .fade-in {
      opacity: 0;
      transform: translateY(30px);
      transition: all 0.8s ease-out;
    }

    .fade-in.appear {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<body>

  <div class="container">
    <header>
      <h1>Создай свою игру на Unity за 20 минут 🎮</h1>
      <p class="subtitle">Всего <span class="highlight">2 урока</span> — и ты сможешь делать игры и использовать нейросети как профи. Без опыта. Без сложного кода. Просто, быстро, мощно.</p>
    </header>

    <div class="lessons">
      <!-- Урок 1 -->
      <div class="lesson-card fade-in">
        <div class="lesson-icon">🧠</div>
        <h3 class="lesson-title">Нейросети для геймдева</h3>
        <p class="lesson-desc">Научишься писать идеальные промты, выбирать нужные нейросети и генерировать ассеты, сценарии, код и идеи для игр за секунды.</p>
        <div class="price">999 ₽</div>
        <button class="btn pulse" onclick="buyLesson(1)">Купить урок</button>
      </div>

      <!-- Урок 2 -->
      <div class="lesson-card fade-in">
        <div class="lesson-icon">🕹️</div>
        <h3 class="lesson-title">Unity за 20 минут</h3>
        <p class="lesson-desc">Пошагово соберём игру с нуля: импорт ассетов, настройка сцен, скрипты, публикация. Даже если ты никогда не открывал Unity!</p>
        <div class="price">999 ₽</div>
        <button class="btn pulse" onclick="buyLesson(2)">Купить урок</button>
      </div>
    </div>

    <div style="text-align: center; margin: 60px 0;">
      <p style="color: #aaa; margin-bottom: 20px;">Или возьми оба урока со скидкой!</p>
      <div class="price" style="font-size: 2.5rem;">1499 ₽</div>
      <button class="btn pulse" style="padding: 20px 40px; font-size: 1.2rem;" onclick="buyLesson('bundle')">
        Взять оба урока ➡️
      </button>
    </div>
  </div>

  <!-- Модальное окно -->
  <div id="thankYouModal" class="modal">
    <div class="modal-content">
      <h2>🎉 Спасибо за покупку!</h2>
      <p>Ссылка на уроки отправлена на вашу почту. Готовьтесь творить игры!</p>
      <button class="btn close-btn" onclick="closeModal()">Понятно</button>
    </div>
  </div>

  <footer>
    © 2025 GameDev Express. Все права защищены. Создано с любовью к играм 🎮
  </footer>

  <script>
    // Анимация появления при скролле
    document.addEventListener("DOMContentLoaded", () => {
      const fadeElements = document.querySelectorAll('.fade-in');
      
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('appear');
          }
        });
      }, { threshold: 0.1 });

      fadeElements.forEach(el => observer.observe(el));
    });

    // Модальное окно
    function buyLesson(lesson) {
      document.getElementById('thankYouModal').style.display = 'flex';
      console.log(`Покупка урока: ${lesson}`);
    }

    function closeModal() {
      document.getElementById('thankYouModal').style.display = 'none';
    }

    // Закрытие по Esc
    document.addEventListener('keydown', (e) => {
      if (e.key === "Escape") closeModal();
    });
  </script>

</body>
</html>
